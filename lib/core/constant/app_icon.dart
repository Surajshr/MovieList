import 'package:flutter/material.dart';
import 'package:listmovie/core/constant/app_color.dart';

class AppIcons{

  static const backButton = Icon(
    Icons.arrow_back,
    color: AppColors.primaryColor,
  );

  static const close = Icon(Icons.close);
  static const time = Icon(Icons.access_alarm_sharp);

}